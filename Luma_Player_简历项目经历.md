# Qt + FFmpeg + OpenGL 局域网同看播放器 独立开发者 | 2026-05 - 至今

## 项目描述

本项目是一个基于 `Qt 6 + FFmpeg + OpenGL` 开发的桌面播放器，支持本地视频播放、播放列表、倍速、音量、全屏、动态 UI，并扩展了局域网同看房间、实时聊天室、视频文件传输和播放进度同步能力。项目采用 UI、播放控制、解码线程、音频输出、OpenGL 渲染和网络通信分层设计，重点解决了音视频解码渲染、播放状态管理和多人同看同步问题。

## 技术栈

`C++17`、`Qt 6`、`Qt Widgets`、`Qt Multimedia`、`Qt Network`、`QThread`、`QTimer`、`FFmpeg`、`OpenGL`、`GLSL Shader`、`CMake`、`MinGW`、`JSON`、`TCP Socket`

## 核心工作

1. 基于 FFmpeg 实现媒体解封装、音视频软解码和音频重采样，通过独立工作线程和线程安全队列连接解码、音频输出与视频渲染流程，避免 UI 主线程阻塞。

2. 基于 `QOpenGLWidget` 和 GLSL Shader 实现 `YUV420P -> RGB` 视频渲染，支持窗口缩放、画面铺满和首帧快速显示，并处理 OpenGL 兼容、纹理上传和黑屏问题。

3. 设计播放器状态机和控制逻辑，支持播放、暂停、停止、进度跳转、倍速、音量、全屏、播放结束自动下一条等功能，并完成播放列表持久化、批量添加、拖拽导入和右键菜单操作。

4. 基于 `Qt Network` 实现局域网同看房间，支持房主创建房间、成员通过 IP 加入、成员列表同步和实时聊天室，使用 JSON 消息协议处理聊天、成员状态和播放控制同步。

5. 实现房主视频文件分块传输和观众端自动接收播放，观众加入房间后可自动获取当前视频、进度和倍速，无需手动选择文件即可进入同看状态。

6. 优化同看同步体验，房主播放中以 `250ms` 间隔同步播放进度，观众端根据偏差进行平滑校正，并通过保留当前 OpenGL 帧减少同步 seek 时的黑屏闪烁。

## 项目亮点

完整覆盖了本地播放器核心链路，并在此基础上扩展出局域网同看、聊天室、视频文件传输和播放同步能力；项目模块边界清晰，便于后续扩展硬件解码、字幕、多音轨、公网房间和更高效的二进制传输协议。
