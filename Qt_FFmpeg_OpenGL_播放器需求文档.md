# Luma Player 需求文档

## 1. 文档目标

本文档用于描述 `Luma Player` 项目的当前需求范围、功能边界、技术架构、非功能指标、验收标准和后续演进方向。

本项目已经从最初的 `Qt + FFmpeg + OpenGL` 本地播放器，扩展为一个支持本地播放、播放列表、倍速、动态界面、局域网同看、实时聊天室和视频文件传输的桌面播放器原型。

## 2. 项目定位

### 2.1 项目类型

桌面端本地媒体播放器 + 局域网同看聊天室。

### 2.2 项目目标

- 构建一个可本地运行的 Qt 桌面播放器。
- 使用 FFmpeg 完成媒体解封装、音视频软解码和音频重采样。
- 使用 OpenGL 完成视频渲染。
- 支持播放列表、倍速、音量、全屏、拖拽导入等常见播放器交互。
- 支持在同一局域网内创建同看房间，邀请其他用户加入。
- 支持房间内实时聊天。
- 支持房主向房间成员发送视频文件，成员接收后自动播放。
- 支持房主播放控制同步，包括播放、暂停、停止、进度跳转和倍速。

### 2.3 目标平台

- 当前目标平台：`Windows 10/11`
- 当前开发环境：`Qt 6.10.1 + MinGW + FFmpeg 8.1`
- 后续可扩展平台：`Linux`

### 2.4 目标用户

- 音视频开发学习者
- Qt 桌面开发学习者
- 需要轻量本地播放器的用户
- 需要演示“多人同看 + 聊天”原型的用户

## 3. 当前版本范围

### 3.1 已实现范围

当前版本重点覆盖以下能力：

- 本地视频文件播放
- FFmpeg 解封装和软解码
- Qt 音频输出
- OpenGL 视频渲染
- 播放、暂停、停止、进度跳转
- 音量调节和静音
- 全屏切换
- `0.75 / 1.0 / 1.25 / 1.5 / 2.0` 倍速播放
- 播放列表持久化
- 单击、双击播放列表视频
- 上一首、下一首
- 删除单个视频
- 清空播放列表
- 播放结束后自动播放下一条
- 播放列表右键菜单
- 拖拽文件导入播放列表
- 动态背景和现代化播放器界面
- 局域网创建同看房间
- 通过 IP 加入同看房间
- 房间成员列表
- 房间实时聊天
- 房主视频文件分块发送
- 客户端接收视频并缓存播放
- 房主播放状态同步
- 本地日志输出

### 3.2 当前不包含范围

以下能力不作为当前版本交付要求：

- 公网邀请和 NAT 穿透
- 账号系统
- 云端房间服务器
- DRM 内容播放
- 在线字幕下载
- 直播推流
- 多人同时控制权限管理
- 断点续传
- 大文件高速传输优化
- 硬件解码
- 多音轨和字幕轨切换
- 完整安装包发布

## 4. 典型使用场景

### 场景 1：本地播放视频

用户启动播放器，点击打开按钮选择本地视频文件。播放器加载媒体信息，开始播放视频并输出声音。

### 场景 2：管理播放列表

用户可以通过打开文件、拖拽文件、右键菜单等方式维护播放列表。关闭程序后，播放列表会保存到本地，下次打开时自动恢复。

### 场景 3：倍速播放

用户可以选择 `0.75 倍速`、`1.0 倍速`、`1.25 倍速`、`1.5 倍速`、`2.0 倍速`，播放器根据当前倍速继续播放。

### 场景 4：创建同看房间

房主点击“创建”，输入昵称后启动局域网房间。界面展示房间地址，其他用户可以使用该 IP 加入。

### 场景 5：加入同看房间

观看者点击“加入”，输入房主 IP 和昵称后进入房间。加入后可以看到在线成员列表，并可以在聊天室中实时交流。

### 场景 6：房主发送视频并一起观看

房主打开本地视频后，程序将视频分块发送给加入房间的成员。成员端接收完成后自动缓存并开始播放。

### 场景 7：同步播放控制

房主执行播放、暂停、停止、拖动进度条或切换倍速时，房间成员端同步执行对应操作。

## 5. 功能需求

### 5.1 本地媒体播放

#### FR-001 打开本地媒体文件

- 用户可以通过按钮打开本地媒体文件。
- 文件选择器应过滤常见媒体格式。
- 成功选择后应自动进入播放流程。

#### FR-002 拖拽导入

- 用户可以将本地媒体文件拖入播放器窗口。
- 支持的媒体文件应自动加入播放列表。
- 第一个有效文件可以自动开始播放。

#### FR-003 视频解码

- 使用 FFmpeg 完成媒体解封装和视频软解码。
- 当前重点支持常见 `H.264 + MP4` 文件。

#### FR-004 音频输出

- 使用 Qt 音频输出模块播放音频。
- 应支持常见音频格式转换和重采样。

#### FR-005 OpenGL 渲染

- 使用 OpenGL 渲染视频画面。
- 支持 `YUV420P` 到屏幕显示的渲染路径。
- 视频区域采用铺满模式，允许轻微裁切以避免两侧黑边。

### 5.2 播放控制

#### FR-006 播放与暂停

- 用户可以切换播放和暂停。
- 暂停时音视频时钟停止推进。
- 继续播放时从当前位置恢复。

#### FR-007 停止播放

- 用户可以停止当前媒体。
- 停止后进度归零，播放器进入停止状态。

#### FR-008 进度跳转

- 用户可以拖动进度条进行跳转。
- 跳转后播放器应恢复播放并重新同步音视频。

#### FR-009 倍速播放

- 支持以下倍速：
- `0.75 倍速`
- `1.0 倍速`
- `1.25 倍速`
- `1.5 倍速`
- `2.0 倍速`

#### FR-010 音量与静音

- 用户可以调节音量。
- 用户可以一键静音和恢复音量。

#### FR-011 全屏切换

- 用户可以进入全屏。
- 用户可以退出全屏。

### 5.3 播放列表

#### FR-012 播放列表显示

- 右侧区域展示已添加的视频。
- 列表项显示文件名。
- 鼠标悬停或右键操作应能定位对应文件。

#### FR-013 播放列表持久化

- 播放列表保存到本地配置文件。
- 程序重启后自动恢复播放列表。
- 保存当前选中文件和倍速设置。

#### FR-014 列表播放

- 单击或双击列表项可播放对应视频。
- 支持上一首和下一首。
- 当前视频播放结束后自动播放下一条。

#### FR-015 列表管理

- 支持删除当前视频。
- 支持清空播放列表。
- 支持右键菜单操作：
- 播放
- 删除
- 清空列表
- 在资源管理器中打开

### 5.4 同看房间

#### FR-016 创建房间

- 用户可以创建局域网同看房间。
- 创建时需要输入昵称。
- 默认监听端口为 `45454`。
- 创建成功后显示本机可用 IP 和端口。

#### FR-017 加入房间

- 用户可以输入房主 IP 加入房间。
- 加入时需要输入昵称。
- 加入成功后显示连接状态。

#### FR-018 离开房间

- 用户可以主动离开房间。
- 离开后清空在线成员状态。

#### FR-019 成员列表

- 房间面板展示当前在线成员。
- 成员加入或离开时应更新列表。

### 5.5 聊天室

#### FR-020 实时聊天

- 房间成员可以发送文本消息。
- 消息实时显示在聊天室区域。
- 本地消息和远端消息应有视觉区分。

#### FR-021 系统消息

- 创建房间、加入房间、成员进出、文件接收等事件应以系统消息提示。

### 5.6 视频文件传输

#### FR-022 房主发送视频

- 房主打开视频后，程序自动向房间成员发送视频文件。
- 文件通过 TCP 连接分块传输。
- 传输协议使用 JSON 消息承载文件元信息和分块内容。

#### FR-023 客户端接收视频

- 客户端接收视频分块后保存到本地缓存目录。
- 接收过程中显示接收进度。
- 接收完成后自动加入播放列表并播放。

#### FR-024 缓存路径

- 接收的视频保存到应用数据目录下的 `received_media` 文件夹。
- 如果同名文件已存在，允许覆盖。

### 5.7 同步播放

#### FR-025 打开视频同步

- 房主打开视频后，同步当前媒体信息给房间成员。
- 成员如果本地已存在同路径文件，可以直接打开。
- 成员如果不存在同路径文件，则等待文件传输完成后播放。

#### FR-026 播放状态同步

- 房主播放、暂停、停止时，成员端同步执行。

#### FR-027 进度同步

- 房主拖动进度条后，成员端同步跳转到对应位置。

#### FR-028 倍速同步

- 房主切换倍速后，成员端同步设置对应倍速。

## 6. 非功能需求

### 6.1 性能要求

#### NFR-001 启动时间

- 应用冷启动目标小于 `3 秒`。

#### NFR-002 首帧时间

- 本地媒体首帧显示目标小于 `1.5 秒`。

#### NFR-003 播放流畅性

- 在常见 `720p/1080p H.264` 文件下应基本流畅播放。

#### NFR-004 文件传输性能

- 局域网内短视频文件应能稳定传输。
- 当前版本优先保证正确性，不追求大文件高速传输。

### 6.2 稳定性要求

#### NFR-005 稳定运行

- 打开、切换、停止多个媒体文件时不应崩溃。
- 网络连接断开时不应导致程序崩溃。

#### NFR-006 资源释放

- 切换文件、关闭窗口、离开房间时应释放线程、socket、音频、解码和渲染资源。

### 6.3 易用性要求

#### NFR-007 操作清晰

- 播放控制按钮应清晰可见。
- 聊天、房间、播放列表区域应区分明确。

#### NFR-008 状态可感知

- 播放状态、房间状态、传输进度和错误信息应有明确提示。

### 6.4 可维护性要求

#### NFR-009 模块解耦

- UI、播放控制、解码、渲染、音频输出、播放列表和网络同看逻辑应保持相对独立。

#### NFR-010 日志能力

- 程序应输出关键运行日志，便于定位播放、网络、文件传输和状态同步问题。

## 7. 技术需求

### 7.1 技术选型

- UI 框架：`Qt Widgets`
- 网络通信：`Qt Network`
- 解封装和解码：`FFmpeg`
- 视频渲染：`OpenGL`
- 音频输出：`Qt Multimedia`
- 构建系统：`CMake`
- 编程语言：`C++17`

### 7.2 当前工程模块

当前项目主要模块如下：

- `src/ui`
- 负责主界面、动态背景、播放列表、聊天室和用户交互。
- `src/player`
- 负责播放控制、播放状态、音视频时钟和 FFmpeg 工作线程管理。
- `src/audio`
- 负责 Qt 音频输出设备适配。
- `src/render`
- 负责 OpenGL 视频渲染。
- `src/network`
- 负责同看房间、聊天、成员列表、播放同步和视频文件传输。

### 7.3 网络协议要求

当前同看功能基于 TCP 长连接，消息以 JSON 行为单位传输。

协议消息类型包括：

- `hello`
- 用户加入时发送昵称。
- `peers`
- 同步在线成员列表。
- `chat`
- 传输聊天消息。
- `media`
- 同步当前播放媒体路径和播放状态。
- `playback`
- 同步播放、暂停、停止、seek、倍速等控制命令。
- `file-start`
- 开始传输视频文件，包含文件名、大小、播放位置和倍速。
- `file-chunk`
- 视频文件分块内容，当前使用 Base64 编码承载。
- `file-end`
- 文件传输完成。

### 7.4 文件传输要求

- 文件分块大小当前为 `48 KB`。
- 文件分块经 Base64 编码后放入 JSON 消息。
- 当前方案适合局域网短视频和演示场景。
- 后续大文件传输可升级为二进制协议或独立传输通道。

### 7.5 数据持久化

播放列表状态保存到应用数据目录，内容包括：

- 播放列表路径
- 当前选中文件
- 当前倍速

接收的视频文件保存到应用数据目录下的 `received_media` 文件夹。

## 8. UI/UX 需求

### 8.1 主界面结构

主界面由以下区域组成：

- 顶部品牌和媒体信息区
- 中央视频播放区
- 悬浮播放控制栏
- 右侧播放列表
- 右侧同看房间和聊天室
- 动态背景

### 8.2 播放控制栏

播放控制栏应包含：

- 打开文件
- 上一首
- 播放/暂停
- 下一首
- 停止
- 音量
- 全屏
- 播放进度
- 当前时间/总时长
- 播放状态

### 8.3 播放列表区域

播放列表区域应包含：

- 播放列表标题
- 倍速下拉框
- 删除按钮
- 清空按钮
- 视频列表

### 8.4 同看房间区域

同看房间区域应包含：

- 创建房间按钮
- 加入房间按钮
- 离开房间按钮
- 房间状态
- 在线成员列表
- 聊天消息展示区
- 聊天输入框
- 发送按钮

### 8.5 动态视觉要求

- 背景应具备轻量动态效果。
- 控件应保持清晰可读，避免暗色背景导致按钮不明显。
- 视频区域播放时应突出画面本身，非播放区域保持柔和视觉风格。

## 9. 状态管理需求

### 9.1 播放状态

播放器至少支持：

- `Idle`
- `Loading`
- `Playing`
- `Paused`
- `Stopped`
- `Seeking`
- `EndOfFile`
- `Error`

### 9.2 房间状态

同看房间至少支持：

- `Offline`
- `Host`
- `Client`

### 9.3 同步状态

接收远端同步命令时，应避免再反向广播同一个命令，防止同步循环。

## 10. 媒体格式支持

### 10.1 当前建议支持

封装格式：

- `mp4`
- `mkv`
- `avi`
- `mov`
- `mp3`
- `aac`

视频编码：

- `H.264`
- `H.265` 作为兼容目标

音频编码：

- `AAC`
- `MP3`

### 10.2 支持原则

- 实际支持能力依赖 FFmpeg 构建版本。
- 当前重点保证常见 `MP4 + H.264 + AAC` 文件稳定播放。

## 11. 构建与运行需求

### 11.1 推荐环境

- 操作系统：`Windows 11`
- Qt：`6.10.1`
- 编译器：`MinGW 13.1`
- FFmpeg：`8.1 full shared build`
- CMake：`3.24+`

### 11.2 构建方式

项目应支持通过脚本构建：

```powershell
.\build_local.ps1
```

### 11.3 运行方式

项目应支持直接运行：

```powershell
.\run_local.ps1
```

也应支持传入视频路径：

```powershell
.\run_local.ps1 "D:\path\to\video.mp4"
```

### 11.4 Qt Creator 打开方式

为避免中文路径导致 CMake/Ninja/Qt Creator 不稳定，建议通过英文联接路径打开：

```powershell
.\open_in_qtcreator.ps1
```

推荐 Kit：

- `Qt 6.10.1 MinGW 64-bit`

## 12. 测试需求

### 12.1 本地播放测试

- 打开 `mp4` 文件并播放。
- 暂停后继续播放。
- 拖动进度条跳转。
- 切换倍速。
- 调整音量和静音。
- 切换全屏。
- 播放结束自动下一条。

### 12.2 播放列表测试

- 添加多个视频。
- 删除当前视频。
- 清空列表。
- 单击和双击播放。
- 右键菜单操作。
- 重启程序后播放列表恢复。

### 12.3 同看房间测试

单机测试方式：

- 启动两个播放器进程。
- 第一个创建房间。
- 第二个加入 `127.0.0.1`。
- 在两个窗口之间发送聊天消息。
- 房主打开视频，验证客户端接收并播放。
- 房主暂停、播放、seek、切倍速，验证客户端同步。

局域网测试方式：

- 两台电脑连接同一 Wi-Fi。
- 房主创建房间。
- 另一台电脑输入房主显示的 IP 加入。
- 重复聊天、文件传输和播放同步测试。

### 12.4 异常测试

- 加入不存在的 IP。
- 房主关闭程序后客户端状态提示。
- 传输过程中断开连接。
- 打开损坏视频文件。
- 接收端缓存目录不可写。

## 13. 风险与限制

### R-001 局域网限制

当前同看功能是局域网 TCP 直连，不支持公网穿透。不同网络环境下邀请可能失败。

应对方向：

- 后续增加服务器中转。
- 后续增加 WebSocket 服务端。
- 后续探索 NAT 穿透。

### R-002 大文件传输效率

当前文件分块通过 Base64 放入 JSON，易实现但不适合超大视频。

应对方向：

- 改为二进制帧协议。
- 文件传输和聊天控制拆分为不同通道。
- 增加断点续传和进度取消。

### R-003 同步精度

当前同步以房主操作事件为主，不做高频时钟校准。

应对方向：

- 周期性同步主机播放时间。
- 客户端根据延迟做轻量校正。
- 加入网络延迟估计。

### R-004 播放倍速稳定性

倍速播放涉及音频重采样、时钟和 seek，某些媒体格式下可能出现不同步或卡顿。

应对方向：

- 优化播放速率切换逻辑。
- 增加更细粒度日志。
- 拆分音频变速和视频时钟策略。

### R-005 UI 复杂度增长

播放列表、聊天室、同看控制放在一个窗口中，界面容易拥挤。

应对方向：

- 后续引入可折叠侧栏。
- 聊天室和播放列表做标签页切换。
- 全屏播放时自动隐藏侧栏。

## 14. 里程碑

### M1 单机播放器 MVP

交付内容：

- Qt + CMake 工程
- FFmpeg 接入
- 本地文件打开
- 音视频解码
- OpenGL 渲染
- 播放、暂停、停止、seek

当前状态：已完成。

### M2 播放器增强

交付内容：

- 播放列表
- 倍速
- 音量和静音
- 全屏
- 动态 UI
- 拖拽导入
- 列表持久化

当前状态：已完成。

### M3 局域网同看

交付内容：

- 创建房间
- 加入房间
- 成员列表
- 实时聊天
- 播放控制同步

当前状态：已完成。

### M4 视频文件传输

交付内容：

- 房主发送视频文件
- 客户端接收进度
- 接收完成自动播放

当前状态：已完成第一版。

### M5 同看体验优化

计划内容：

- 文件传输取消和重试
- 大文件传输优化
- 周期性时间校准
- 侧栏折叠
- 房间邀请链接
- 防火墙和端口提示

当前状态：待开发。

## 15. 验收标准

当前版本满足以下条件即可视为阶段验收通过：

- 程序可在 Windows 本地正常启动。
- 可打开常见 `mp4` 视频并播放。
- 视频画面可正常显示。
- 音频可正常输出。
- 支持播放、暂停、停止、seek、音量、全屏和倍速。
- 播放列表可添加、删除、清空、持久化。
- 可启动两个播放器进程进行本机房间测试。
- 一个进程创建房间，另一个进程可加入。
- 两端可以互发聊天消息。
- 房主打开视频后，客户端可接收视频文件并自动播放。
- 房主播放控制能同步到客户端。
- 网络异常和播放异常不会导致程序直接崩溃。

## 16. 当前目录结构

```text
QtFfmpegOpenGLPlayer/
  CMakeLists.txt
  CMakePresets.json
  README.md
  build_local.ps1
  run_local.ps1
  open_in_qtcreator.ps1
  resources.qrc
  assets/
    app_icon.svg
  src/
    main.cpp
    audio/
      AudioBufferDevice.h
      AudioBufferDevice.cpp
    network/
      WatchPartyManager.h
      WatchPartyManager.cpp
    player/
      PlaybackTypes.h
      PlaybackTypes.cpp
      PlayerController.h
      PlayerController.cpp
      PlayerWorker.h
      PlayerWorker.cpp
      SharedAudioBuffer.h
      SharedAudioBuffer.cpp
      VideoFrameQueue.h
      VideoFrameQueue.cpp
    render/
      VideoWidget.h
      VideoWidget.cpp
    ui/
      AnimatedBackgroundWidget.h
      AnimatedBackgroundWidget.cpp
      MainWindow.h
      MainWindow.cpp
```

## 17. 后续优化建议

### 17.1 同看功能

- 增加邀请链接复制按钮。
- 增加端口设置。
- 增加连接质量提示。
- 增加房主权限控制。
- 增加客户端申请控制播放。
- 增加周期性播放时间校准。

### 17.2 文件传输

- 改造为二进制分块协议。
- 增加传输取消。
- 增加失败重试。
- 增加断点续传。
- 增加接收文件去重。

### 17.3 播放能力

- 增加硬件解码。
- 增加字幕加载。
- 增加多音轨切换。
- 增加截图。
- 增加播放历史。

### 17.4 界面体验

- 侧栏支持折叠。
- 播放时隐藏聊天和列表。
- 全屏时显示浮动聊天室。
- 播放列表显示缩略图和时长。
- 聊天消息显示时间。

## 18. 结论

`Luma Player` 当前已经具备从本地播放器到局域网同看播放器的核心原型能力。项目技术链路覆盖 `Qt Widgets`、`FFmpeg`、`OpenGL`、`Qt Multimedia` 和 `Qt Network`，既能作为音视频学习项目，也能继续演进为多人观影桌面应用。

当前最值得继续优化的方向是文件传输效率、同步精度和同看房间的易用性。
