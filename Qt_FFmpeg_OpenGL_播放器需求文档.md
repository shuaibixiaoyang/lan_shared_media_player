# LAN Shared Media Player 需求文档

## 1. 项目概述

`LAN Shared Media Player` 是一个基于 `Qt 6 + FFmpeg + OpenGL` 的桌面端局域网同看播放器。项目覆盖本地媒体播放、播放列表管理、局域网房间、实时聊天、视频文件传输和播放状态同步。

本项目的核心目标是自主实现播放器主要链路，而不是直接封装现成播放器控件：

- 使用 FFmpeg 完成解封装、音视频软解码和音频重采样。
- 使用 Qt Multimedia 输出音频。
- 使用 OpenGL Shader 渲染 YUV 视频帧。
- 使用 Qt Network 实现局域网房间、聊天、文件传输和播放同步。

## 2. 目标用户

- 学习 Qt 桌面开发的用户。
- 学习 FFmpeg 音视频解码流程的用户。
- 需要演示局域网同看能力的课程设计或项目用户。
- 希望在局域网内共享视频并同步观看的轻量场景用户。

## 3. 当前版本范围

### 3.1 已实现功能

- 本地媒体文件播放。
- FFmpeg 解封装和音视频软解码。
- Qt 音频输出和音量控制。
- OpenGL 视频帧渲染。
- 播放、暂停、停止、进度跳转。
- 静音、全屏和倍速播放。
- 播放列表添加、删除、清空、上一首、下一首和自动下一条。
- 播放列表本地持久化。
- 多文件选择和拖拽导入。
- 播放列表右键菜单。
- 局域网创建房间和加入房间。
- 房间成员列表和连接状态展示。
- 房间文本聊天和系统消息。
- 房主视频文件分块发送。
- 观众端自动接收、缓存并播放视频。
- 房主播放、暂停、seek、停止和倍速同步。
- 观众端同步平滑校正，降低频繁 seek 导致的闪烁。

### 3.2 暂不包含

- 公网房间。
- 账号系统。
- 云端服务器。
- NAT 穿透。
- 硬件解码。
- 字幕加载。
- 多音轨切换。
- 大文件断点续传。
- 完整安装包和自动更新。

## 4. 功能需求

### FR-001 本地媒体播放

用户可以打开本地媒体文件并播放。播放器应支持常见视频和音频封装格式，实际解码能力由 FFmpeg 构建版本决定。

验收标准：

- 可以通过按钮选择文件播放。
- 可以通过拖拽导入媒体文件。
- 视频画面和音频输出正常。
- 打开无效文件时应提示错误，不应崩溃。

### FR-002 播放控制

播放器应支持播放、暂停、停止、seek、音量、静音、全屏和倍速控制。

验收标准：

- 播放状态切换后 UI 状态同步更新。
- seek 后画面和声音能恢复到目标位置。
- 倍速切换后继续从当前附近位置播放。

### FR-003 播放列表

播放器应支持多文件添加、拖拽导入、单击/双击播放、上一首/下一首、删除、清空、右键菜单和播放结束自动下一条。

验收标准：

- 多文件可以批量进入播放列表。
- 删除当前项后可以自动切换到合理的下一项。
- 程序重启后可以恢复上次播放列表。

### FR-004 视频渲染

播放器应使用 OpenGL 渲染视频帧，并支持 `YUV420P -> RGB` Shader 转换。

验收标准：

- 视频帧可正常显示。
- 窗口缩放后画面继续保持合理比例。
- 无视频时显示空状态背景。

### FR-005 局域网房间

用户可以创建同看房间，其他用户可以通过房主 IP 加入。

验收标准：

- 房主可以创建房间并显示局域网地址。
- 观众输入 IP 后可以加入。
- 成员加入或离开后成员列表更新。
- 网络错误应提示用户。

### FR-006 实时聊天

房间成员可以发送文本消息，聊天消息应实时显示。

验收标准：

- 本地消息、远端消息和系统消息可区分。
- 空消息不发送。
- 断开房间后不能继续发送聊天。

### FR-007 视频文件传输

房主打开视频后，应将当前视频发送给观众端。观众端接收完成后保存到本地缓存目录，并自动播放。

验收标准：

- 观众端能看到接收进度。
- 接收完成后自动加入播放列表。
- 接收文件保存在应用数据目录，避免污染源视频目录。

### FR-008 播放同步

房主播放、暂停、停止、seek 和切换倍速时，应同步到观众端。

验收标准：

- 新观众加入后能自动接收当前视频和播放状态。
- 房主操作后观众端执行对应操作。
- 周期同步不会频繁打断观众端播放。

## 5. 非功能需求

### NFR-001 性能

- 应用冷启动目标小于 `3 秒`。
- 本地视频首帧显示目标小于 `1.5 秒`。
- 常见 `720p / 1080p H.264` 视频应基本流畅播放。
- 局域网短视频传输应稳定完成。

### NFR-002 稳定性

- 打开、切换和停止多个文件时程序不应崩溃。
- 网络断开、加入失败或文件传输失败时程序不应崩溃。
- 关闭程序时应释放解码线程、音频设备、OpenGL 资源和 socket。

### NFR-003 可维护性

- UI、播放控制、解码、音频输出、视频渲染和网络同看逻辑应保持模块化。
- 关键函数应保留说明性注释，便于答辩、复盘和后续维护。
- 构建脚本和运行脚本应保持简单可读。

### NFR-004 易用性

- 视频区域、播放控制、播放列表和同看聊天应清晰分区。
- 常用本地播放操作应优先可见。
- 错误、房间状态和文件传输进度应有明确提示。

## 6. 技术架构

### 6.1 模块划分

```text
src/
  audio/      QAudioSink 读取适配
  network/    局域网房间、聊天、文件传输、播放同步
  player/     播放控制器、FFmpeg 解码线程、音视频队列
  render/     OpenGL 视频显示控件
  ui/         主窗口、播放列表、同看聊天、动态背景
```

### 6.2 播放链路

```text
UI 操作
  -> PlayerController
  -> PlayerWorker
  -> FFmpeg 解封装/解码
  -> SharedAudioBuffer
  -> QAudioSink 输出声音
  -> VideoFrameQueue
  -> VideoWidget OpenGL 渲染画面
```

### 6.3 同看链路

```text
Host QTcpServer
  -> Guest QTcpSocket
  -> JSON 行协议
  -> 聊天 / 成员列表 / 文件传输 / 播放同步
```

### 6.4 网络消息类型

- `hello`：成员加入时发送昵称。
- `peers`：同步成员列表。
- `chat`：聊天消息。
- `media`：当前媒体路径和播放信息。
- `playback`：播放、暂停、停止、seek、倍速和周期同步。
- `file-start`：开始传输视频文件。
- `file-chunk`：视频文件分块。
- `file-end`：文件传输完成。

## 7. 构建与运行

### 7.1 推荐环境

- Windows 10/11
- Qt `6.10.1`
- MinGW `13.1`
- CMake `3.24+`
- FFmpeg `8.1 full shared build`

默认 FFmpeg 路径：

```text
C:/ffmpeg-dev/ffmpeg-8.1-full_build-shared
```

### 7.2 构建

```powershell
.\build_local.ps1
```

### 7.3 运行

```powershell
.\run_local.ps1
```

指定视频启动：

```powershell
.\run_local.ps1 "D:\media\sample.mp4"
```

### 7.4 Qt Creator

建议通过脚本打开项目，避免中文路径导致 CMake/Ninja 兼容问题：

```powershell
.\open_in_qtcreator.ps1
```

推荐 Kit：

```text
Qt 6.10.1 MinGW 64-bit
```

## 8. 测试用例

### TC-001 本地播放

打开 `mp4` 文件，验证画面、声音、暂停、继续、seek、音量和全屏。

### TC-002 播放列表

一次选择多个视频，验证全部进入播放列表，并自动播放第一个有效文件。

### TC-003 拖拽导入

拖拽多个视频到窗口，验证有效媒体文件进入播放列表。

### TC-004 同看房间

启动两个播放器进程，一个创建房间，另一个输入 `127.0.0.1` 加入，验证成员列表和聊天。

### TC-005 视频传输

房主打开视频，验证观众端自动接收、缓存并播放。

### TC-006 播放同步

房主播放、暂停、seek、停止和切换倍速，验证观众端同步执行。

### TC-007 新成员加入

房主先播放视频，再让新成员加入，验证新成员自动进入当前视频和当前进度。

### TC-008 异常处理

验证加入不存在 IP、传输过程中断开、打开损坏视频时程序不崩溃并给出提示。

## 9. 风险与限制

### R-001 局域网限制

当前只支持局域网直连。跨公网使用需要服务器中转或 NAT 穿透。

### R-002 文件传输效率

当前文件传输使用 `JSON + Base64` 分块，适合演示和短视频。大文件建议后续改为二进制协议。

### R-003 同步精度

当前同步基于事件同步和周期校正，无法做到严格帧级同步。后续可加入 RTT 估计、主机时钟对齐和播放速率微调。

### R-004 播放格式兼容

实际格式支持取决于 FFmpeg 构建版本。部分特殊编码、字幕和多音轨暂未完整支持。

## 10. 后续优化

- 文件传输改为二进制协议。
- 增加传输取消、失败重试和断点续传。
- 支持公网房间或中转服务器。
- 增加硬件解码。
- 增加字幕和多音轨。
- 优化同步算法，减少 seek，使用播放速率微调。
- 增加安装包和自动部署脚本。
